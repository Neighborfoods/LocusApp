# API base URL. Set to your machine's LAN IP for simulator/device (most reliable).
# Run: ifconfig | grep "inet " to find your IP (e.g. 192.168.1.100).
# Backend must bind to 0.0.0.0:8080 so simulator can connect.
EXPO_PUBLIC_API_URL=http://192.168.1.1:8080/api/v1
